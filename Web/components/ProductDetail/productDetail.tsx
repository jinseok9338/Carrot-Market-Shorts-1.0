import { FC } from "react";
interface ProductDetailIProps {}

const ProductDetail: FC<ProductDetailIProps> = (props) => {
  return (
    <div>
      <span>Product Description</span>
    </div>
  );
};

export default ProductDetail;
