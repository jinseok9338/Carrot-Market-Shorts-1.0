image:
  file: .gitpod.Dockerfile

ports:
  - port: 8000
    onOpen: open-browser

tasks:
  - name: config Database and npm install
  - init: npm install
  - command: createuser user && createdb mydb
